create a .yml file 
create a readme for what i am doing, include the line where u run the .yml file to create the conda enviroment

double check the mf and hf data coming from sensor, make sure it is good even after the gaps
        checked and everything looked normal

modify the pipline to include the proper mae metric calculation, just get it working. 
    added mae to work nicely with the most calculations, although it is removing the nans (when most did not )

clean up notebooks so that we can hand off to branko

start on west coast data & fino

FOUND A BUG: where the neural network model is being trained on scaled data, tested on scaled data, but metric is being evaluated on non-scaled data


question for tim
    what is a good guess? or an array of guesses
    should we only use 1 guess or can we use guess number 2 if the first guess fails to converge
    